/**
 * Social image dimensions, kept out of the JSX template so schema and metadata
 * modules can import them without pulling the renderer in with them.
 */
export const OG_SIZE = { width: 1200, height: 630 } as const
export const TWITTER_SIZE = { width: 1200, height: 675 } as const
